# سيستم الحسابات - دليل البدء السريع

## نظرة عامة
هذا النظام يدعم الآن التخزين السحابي عبر Firebase Firestore مع نسخ احتياطية محلية في حالة فقدان الاتصال.

## المتطلبات
- متصفح حديث يدعم JavaScript
- اتصال بالإنترنت للوصول إلى Firebase
- مشروع Firebase مع خدمة Firestore مفعلة

## إعداد Firebase
1. افتح `index.html` وابحث عن كائن `window.FIREBASE_CONFIG` في أسفل الملف.
2. تأكد من أن القيم التالية صحيحة لمشروع Firebase الخاص بك:
   - `apiKey`
   - `authDomain`
   - `projectId`
   - `storageBucket`
   - `messagingSenderId`
   - `appId`
3. إذا كان لديك إعداد بيئة مخصص، يمكن للمشروع قراءة متغيرات `window.FIREBASE_ENV` أيضاً.
4. تأكد من تفعيل Firestore في مشروع Firebase، ولا تحتاج إلى أي هيكل ثابت قبل التشغيل.

## كيفية التشغيل
1. افتح `index.html` في المتصفح.
2. انتظر ظهور شارة الحالة في أسفل الصفحة.
   - `☁️ Firebase متصل` تعني أن التطبيق يتصل بالسحابة بشكل صحيح.
   - `💾 تخزين محلي` يعني أن التطبيق يستخدم نسخة محلية احتياطية بسبب عدم توفر Firebase.
3. قم بإضافة بيانات جديدة من شاشة إدارة البيانات.
4. بعد الحفظ، يتحقق النظام تلقائياً من Firestore ويستمر في حفظ السجلات هناك.

## التحقق من عمل التخزين السحابي
1. افتح وحدة تحكم Firebase Firestore في مشروعك.
2. تأكد من وجود المستند `almuheet/data` في مجموعة `almuheet`.
3. تأكد من وجود المستند `almuheet/files` في مجموعة `almuheet` بعد رفع مرفق ملف.
4. عند حفظ سجل جديد، يجب أن يظهر محتوى الـ `data` محدثًا في `almuheet/data`.

## اختبار يدوي سريع
- أضف سجل إيراد جديد مع `رقم العقار` و`اسم المهندس المشرف`.
- تحقق من أن البيانات تُحفظ ولا تظهر رسائل خطأ.
- أضف مرفقات عقد أو سند قبض، ثم افتح الأرشيف وتأكد من ظهورها.
- افتح Firebase Firestore وتحقق من أن `almuheet/data` و`almuheet/files` تحتويان على المحتوى المرسل.

## ملاحظات
- النظام يدعم التخزين السحابي أولاً، و`localStorage` فقط كنسخة احتياطية.
- إذا كان الاتصال بـ Firebase غير متاح، فسيتم استخدام المخزن المحلي تلقائياً إلى أن يعود الاتصال.
- تم إزالة الحقول القديمة `تاريخ السند` و`رقم السند` من إضافة الإيراد الجديد.
